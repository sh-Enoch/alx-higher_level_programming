#!/usr/bin/python3
"""Check if obj is instance of."""


def is_kind_of_class(obj, a_class):
    """Check if isnstance of class specified."""
    if isinstance(obj, a_class):
        return True
    return False
